# RPA Sentinel 自动巡检仓库 Agent 工作规则

## 一、仓库边界

- 本目录 `工具/自动巡检` 是巡检程序的独立软件仓库根目录。
- 上级目录是数字员工治理项目，不是本软件仓库的一部分。不得把上级的治理、工作手册、业务台账、验证记录或迁移记录加入本仓库。
- 需要业务口径时，读取上级 `../../业务规则/11_异常过滤规则.md`；需要影刀接口证据时，先读取上级 `../../影刀API仓库/catalog.json`。
- 本文件约束软件开发；上级 `../../AGENTS.md` 继续约束整个数字员工项目。两者冲突时，用户当前明确指令和上级治理规则优先。

## 二、开工顺序

1. 确认当前工作目录是本仓库根目录。
2. 读取本文件和 `README.md`。
3. 只读取与任务相关的模块和测试，不全量加载运行数据。
4. 修改前说明目标、授权范围、风险和验收方式。
5. 先做最小变更，修改后回读并运行与改动相符的测试。

## 三、架构边界

- `app/`：WPF 界面、托盘、调度触发和本机配置初始化。
- `src/rpa_monitor/`：业务模块。界面不得重新实现 API、规则、AI 或重试逻辑。
- `config/`：可提交的非敏感静态配置和输出结构。
- `tests/`：默认只运行本地测试，不联网，不触发真实影刀写操作。
- `data/`：运行记录、状态、截图和回收站，仅本机使用，禁止提交。
- 顶层旧脚本属于回退资产；除非用户明确要求迁移或删除，不继续在旧脚本上开发。

## 四、配置和敏感信息

- 运行配置和密钥只从仓库根目录 `.env` 读取。
- `.env` 必须被 `.gitignore` 排除；只维护不含真实值的 `.env.example`。
- 不在代码、日志、测试、README、提交信息或聊天输出中写入口令、令牌、Cookie、Access Key、数据库地址或个人联系方式。
- 初始化脚本只允许复制 Windows 用户环境变量，禁止修改或删除原环境变量。
- 输出诊断信息时只报告配置项是否存在，不输出真实值。

## 五、业务规则

- GUI 的巡检时间段和频率是运行时唯一设置来源；默认 08:00—17:30、每 10 分钟一次。
- 排班开关只决定当天是否执行；启用时每天最多读取一次飞书排班，关闭时每天按 GUI 设置执行。
- 首次扫描完整读取昨天 17:30 至当前时间，后续扫描只读取最近 50 条。
- `10min_`、`20min_` 开头的循环任务连续异常达到 5 次才进入“需关注异常”，并且禁止调用 Codex 和自动重试。
- 飞书“影刀消息”当前不读取，不得自行恢复。
- `robotClientName` 是执行机器人；`robotName` 是 RPA 应用名称，不得混用。
- 高级任务通过 API 调用时可能没有 `taskName`。此时保留原始字段，以 `robotName` 作为显示名称，并在独立“任务类型”列标注“高级任务”；不得显示为空，也不得把回退值写回原始 `taskName`。
- 状态、摘要、截图和 AI 结论必须分开保存；截图缺失不是接口失败。
- “已解决”只改变展示状态：记录必须继续保留在“需关注异常”中并灰色淡化，只有“放入回收站”才从异常列表移除。
- 截图预览必须支持点击打开大图；回收站操作必须选中右键目标行、成功后立即刷新，并捕获重复操作错误，禁止未处理异常导致界面退出。

## 六、AI 与重试安全边界

- AI 分析使用本机 Codex CLI 和 `.env` 中指定的会话、模型，不调用 OpenAI API。
- Codex 只分析证据，不得通过分析提示修改代码、调用工具或执行任务。
- Codex 额度、频率或解析失败时，标记“AI分析失败/待人工判断”，禁止自动重试，30 分钟后再分析。
- AI 自动重试默认关闭。循环任务、黑名单任务、同名任务正在运行、高风险关键词、证据不足、近期无成功记录或达到最大次数时禁止自动重试。
- 手动重试必须显示风险提示并取得当次确认；测试中禁止调用真实重试接口。
- 排期重试当前只是框架。未经用户明确授权，不连接 PostgreSQL、不读取甘特图、不启用排期队列。

## 七、Windows 兼容性

- WPF 脚本兼容 Windows PowerShell 5.1，不使用 PowerShell 7 专属语法。
- WPF 和 Python 子进程统一使用 UTF-8；不得移除 `PYTHONUTF8`、`PYTHONIOENCODING` 或 Python 输出编码设置。
- `.ps1` 使用 UTF-8 BOM；中文 `.vbs` 使用 UTF-16LE，避免双击启动乱码或失效。
- 后台进程必须隐藏控制台窗口。关闭 WPF 只缩到托盘；只有“暂停巡检”暂停扫描，托盘“退出程序”才结束进程。
- 不创建 Windows 计划任务，不为每轮巡检新建 Codex 会话。

## 八、验证要求

- Python 改动至少运行 `python -m unittest discover -s tests -v`。
- WPF 改动必须运行 PowerShell 语法解析；涉及启动时再做一次短暂启动测试。
- 编码改动必须按 WPF 相同的隐藏子进程链路验证中文可读。
- API 只读测试要同时检查 HTTP、`success`、业务 `code` 和 `data`；HTTP 200 不能单独作为成功证据。
- 真实重试、外发消息、数据库写入、发布和权限变更必须单独取得用户授权，不能包含在普通测试中。
- 交付时明确列出：已修改、已测试、未测试、需要用户操作和仍存在的风险。

## 九、Git 与发布

- 不得提交 `.env`、`data/`、截图、日志、回收站、旧运行数据或任何敏感内容。
- 提交或上传前执行敏感信息扫描，并展示目标仓库、分支、文件范围和扫描结论。
- 未经用户对目标仓库和本次上传再次确认，不执行 `git push`、创建远程仓库或发布版本。
- 不改写已有历史，不使用破坏性 Git 命令处理用户改动。
